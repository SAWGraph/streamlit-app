# Hello Streamlit App

Simple minimal Streamlit "Hello World" app.

Run locally:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```

Open the URL printed by Streamlit (usually `http://localhost:8501`).
